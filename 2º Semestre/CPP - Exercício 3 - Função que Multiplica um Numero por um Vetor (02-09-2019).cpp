/*3-faça uma função que recebe como parâmetro um vetor de inteiros e um número inteiro. o vetor deve ser multiplicado pelo número.*/
#include<stdio.h>
int Multipli(int vet[], int Num){
	int i, recebedor[5];
	for(i=0; i<5; i++){
		recebedor[i]=Num*vet[i];
	}
	return(Num);
}

